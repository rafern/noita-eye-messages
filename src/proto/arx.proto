syntax = "proto3";

package noita_eye_messages.proto.arx;

message EncodedArxRound {
    uint32 add = 1;
    uint32 rot = 2;
    uint32 xor = 3;
}

message EncodedArxKey {
    repeated EncodedArxRound rounds = 1;
}